// Previous imports remain the same

export default function Portfolio() {
  const [activeCategory, setActiveCategory] = useState('all')

  return (
    <section className="py-20 bg-[#1f2580] text-white">
      {/* Rest of the component remains the same */}
    </section>
  )
}

